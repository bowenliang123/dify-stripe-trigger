identity:
  name: "stripe_trigger_event"
  author: "bowenliang123"
  label:
    en_US: "StripeTrigger Event"
    zh_Hans: "stripe_trigger 事件"

description:
  en_US: "Occurs when a Checkout Session has been successfully completed."
  zh_Hans: "当 Checkout Session 成功完成时发出。"

output_schema:
  type: object
  properties:
    message:
      type: string
      description: "Sample message returned to downstream workflow nodes."
    raw_event:
      type: object
      description: "Original payload returned by the third-party integration."

extra:
  python:
    source: events/event.py
